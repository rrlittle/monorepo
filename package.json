{
  "name": "monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "config/*",
    "libs/*"
  ],
  "devDependencies": {
    "turbo": "^1.4.5"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:test": "turbo run dev:test",
    "clean": "turbo run clean && rm -rf node_modules"
  }
}
